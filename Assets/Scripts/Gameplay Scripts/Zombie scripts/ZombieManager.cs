using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//sergio's script
//zombie manager, it keeps track of all active zombies and severs the purpose of instantiating new ones when needed.
//it will assign their spwan point, entry point and choose what king of zombie to spwan based on their "rarety"

public class ZombieManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
